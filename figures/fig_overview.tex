% PLACEHOLDER. Previous drawing: fig_overview_prev.tex (stale -- it labels a two-resolution encoder
% and an active descriptor-prediction term, neither of which the current recipe uses).
\figplaceholder{0.96\textwidth}{%
  End-to-end pipeline: Phase-A label-free encoder $\rightarrow$ frozen per-patch, per-sensor bank
  $\rightarrow$ compatibility, admissibility, ranking, voting, sensor fusion.}
